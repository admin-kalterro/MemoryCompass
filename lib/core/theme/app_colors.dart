import 'package:flutter/material.dart';

/// Brand palette. Ink and brass carry the identity; patina is the app's
/// original teal, kept as the secondary accent rather than replaced.
class AppColors {
  const AppColors._();

  static const Color ink = Color(0xFF0D1A2E);
  static const Color ink2 = Color(0xFF16263F);
  static const Color ink3 = Color(0xFF1F3350);

  static const Color brass = Color(0xFFC9974C);
  static const Color brassBright = Color(0xFFE6BB78);

  static const Color patina = Color(0xFF4F8D83);

  static const Color paper = Color(0xFFECE4D1);

  static const Color rust = Color(0xFFB24B34);
}
